# AI Office Base Adapter

The base adapter installs no host-specific wrapper files.

Use:

- `AI-OFFICE.md` for the host-neutral framework rules
- `ai-office` for deterministic task, milestone, status, and validation commands

Install this adapter when you want AI Office without binding the project to a specific coding assistant host.
