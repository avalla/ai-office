id: researcher
role_key: technical-researcher
role: technical-researcher
version: 1
capabilities:
  - inspect_project
  - gather_evidence
  - compare_alternatives
  - report_uncertainty
tools:
  - project.search
  - project.get_active_adrs
  - code.get_dependencies
model_policy: high_reasoning
limits:
  max_iterations: 8
  max_cost_micros: "2000000"
  timeout_seconds: 1800
